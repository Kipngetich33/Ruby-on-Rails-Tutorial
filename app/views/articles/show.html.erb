<h1><%= @article.title %></h1>
<hr>
<p><%= @article.body %></p>
<%= link_to "Edit", edit_article_path(@article) %> | <%= link_to "Destroy", article_path(@article), data: {turbo_method: :delete,
    turbo_confirm: "Are you sure?"
} %>
<h2>Comments</h2>
<hr>
<%= render @article.comments %>

<%# the add comment form is below %>
<%= render 'comments/form' %>

